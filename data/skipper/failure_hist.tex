\begin{figure}[ht]
    \myfloatalign
    \begin{tikzpicture}
    \begin{axis}[
        ybar,
        ymin=0,
        enlarge x limits={abs=0.2},
%        width=\textwidth,
        bar width = 4pt,
%        xtick=data,
%        xticklabels from table={./data/skipper/failure_hist.csv}{segments},
%        x tick label style={rotate=60, anchor=east},
        xlabel={Segment \#},
        ylabel={\# Failures},
        legend style={at={(1.05,1)}, anchor=north west,legend columns=1},]
        \addplot table [x expr=\coordindex, y={fails}]{./data/skipper/failure_hist.csv};
    \end{axis}

    \end{tikzpicture}
    \caption[Number of \acs{HTTP} failures from \expid{S5B5}]{Total number of failed \ac{HTTP} responses (non-200 response codes) in the entire network, from the 5 Skipper experiment (\expid{S5B5}).}
    \label{bar:skipper_fail_hist}
\end{figure}