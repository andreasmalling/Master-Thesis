\chapter{Docker Compose files}
\label{app:compose}

\lstinputlisting[
    language=docker-compose,
    label={lst:compose-exp},
    caption={[Docker compose file for experiment.]A full Docker compose file used for running the stable network and introducing experimental users.}
    ]{code/docker-compose.yml}

\newpage
\lstinputlisting[
    language=docker-compose,
    label={lst:compose-plot},
    caption={[Docker compose file for plotting.]A full Docker compose file used for running the plot service, to evaluate to data collected in an experiment.}
    ]{code/plot-compose.yml}
